# Remaining Tasks for GPS Attendance

## Backend Logic & Verification
- [x] **Leader Override**:
  - Implement server action `manualCheckIn` in `src/app/study/[id]/actions.ts`.
  - Add UI in `src/app/study/[id]/page.tsx` (or a MemberList component) to allow Leader to manually check in members.
